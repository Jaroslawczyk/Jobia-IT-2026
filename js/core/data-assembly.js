/* =============================================================
   Сборка данных
   Подмешивает к каждому направлению из ROLES дополнительные объекты:
   EXTRA (языки, сертификаты…), AI (риск ИИ), TOOLS (железо),
   TRACKS (ветки), FW (фреймворки).
   Должен выполняться ПОСЛЕ загрузки всех файлов js/data/*.
   ============================================================= */

ROLES.forEach(r => { Object.assign(r, EXTRA[r.id] || {});
  r.ai = AI[r.id]; r.toolsNeed = TOOLS[r.id]; r.tracks = (TRACKS[r.id] && TRACKS[r.id].items) ? TRACKS[r.id] : null;
  r.fw = FW[r.id] || null; });
